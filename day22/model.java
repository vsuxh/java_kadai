import java.util.Random;
import java.util.Arrays;
import java.awt.*;
import java.awt.event.*;
import javax.swing.*;
import javax.swing.event.*;
public class model(){
	public model{
		private boolean[][] board = new boolen[4][4];
		private Arrays.fill(board, false);
		private Random rand = new Random();
	}

	private void pressCell[row][col]{
        	if (board[row][col] = true)(board[row][col] = false;)
        	else (board[row][col] = true;)
        }

	private void reset(){
		model();
		//pressCell[row][col]でrand使って複数回実行予定	
	}

	private void isCleared(){
		private boolean isfalse = false;
		if 
	}
}
